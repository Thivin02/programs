#include<stdio.h>
void main(){
    int temp,a[100],count,hash[50],num;  
    for(int j=0;j<count;j++){
        temp=a[j];
        count=0;
        num=0;
        for (int i = 1; i < count; i++)
    {
        if(temp==a[i]){
            count++;
            a[i]=0;
        }
    }
        while (count!=0)
        {
            num=count*10+j;
            /* code */
        }
        
        /* code */
    
    hash[j]=num;
    }    
}